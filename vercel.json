{
    "builds": [
        {"src": "/api/**/*.js", "use": "@vercel/node" },
        {"src": "/client/**", "use": "@vercel/static-build" }
    ],
        
    "rewrites": [{ "source": "/(.*)", "destination": "/" }]
    
}